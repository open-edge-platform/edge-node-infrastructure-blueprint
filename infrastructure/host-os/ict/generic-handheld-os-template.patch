SPDX-FileCopyrightText: (C) 2026 Intel Corporation
SPDX-License-Identifier: Apache-2.0

--- a/ubuntu24-x86_64-minimal-ptl-pv-raw.yml (upstream @ b21499751a1e50677bbf2b4ea4517adcb67da7d8)
+++ b/generic-handheld-os-template.yml
@@ -1,23 +1,8 @@
-# AI-searchable metadata for template discovery
-metadata:
-  description: "Ubuntu 24.04 minimal PTL PV desktop image in raw format for Intel PTL development and validation"
-  use_cases:
-    - "PTL platform validation"
-    - "Intel GPU and NPU enablement"
-    - "desktop development"
-    - "raw image deployment"
-  keywords:
-    - ptl
-    - pv
-    - ubuntu
-    - raw
-    - desktop
-    - intel
-    - gpu
-    - npu
+# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
+# SPDX-License-Identifier: Apache-2.0
 
 image:
-  name: minimal-desktop-ubuntu-ptl-pv
+  name: minimal-desktop-ubuntu
   version: "24.04"
 
 target:
@@ -29,7 +14,7 @@
 # Disk layout and output artifact definition
 disk:
   # Human friendly name logged by builders
-  name: minimal-desktop-ubuntu-ptl-pv
+  name: minimal-desktop-ubuntu
   artifacts:
     # Request conversion to raw
     - type: raw
@@ -79,6 +64,7 @@
 
 # Additional package repositories for this image
 packageRepositories:
+
   # Intel overlay repository (aligned with installer script)
   - codename: "noble"
     url: "https://download.01.org/intel-linux-overlay/ubuntu"
@@ -287,6 +273,35 @@
       - libxdp1
       - intel-media-va-driver-non-free
       - spice-client-gtk
+  # ----- OpenVINO -----
+  - codename: "ubuntu24"
+    url: "https://apt.repos.intel.com/openvino/2025"
+    pkey: "https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB"
+  # ----- Intel oneAPI (oneDNN / Base Toolkit) -----
+  - codename: "all"
+    url: "https://apt.repos.intel.com/oneapi"
+    pkey: "https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB"
+    component: "main"
+    priority: 500
+  # ----- Docker CE official -----
+  - codename: "noble"
+    url: "https://download.docker.com/linux/ubuntu"
+    pkey: "https://download.docker.com/linux/ubuntu/gpg"
+    component: "stable"
+    priority: 500
+  # ----- Intel RealSense SDK -----
+  # ref: https://docs.ros.org/en/iron/p/librealsense2/user_docs/distribution_linux.html
+  # TODO: the key ID below is hardcoded; need to find a better way to handle dynamic key discovery
+  #       in the packageRepositories section (ICT does not support shell substitution here).
+  # To get the current key ID if the build fails with NO_PUBKEY, run:
+  #   curl -sSf "https://librealsense.intel.com/Debian/apt-repo/dists/$(lsb_release -cs)/InRelease" \
+  #     | gpg --status-fd 1 --verify 2>/dev/null | grep "NO_PUBKEY" | awk '{print $3}'
+  # Then update the key ID in the pkey URL below.
+  - codename: "noble"
+    url: "https://librealsense.intel.com/Debian/apt-repo"
+    pkey: "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0xFB0B24895113F120"
+    component: "main"
+    priority: 500
 
 systemConfig:
   name: minimal
@@ -485,7 +500,12 @@
     - build-essential
     - apt-transport-https
     - default-jre
-    - docker-compose
+    # Docker CE engine (pre-installed, disabled by default; cloud-init enables on first boot)
+    - docker-ce
+    - docker-ce-cli
+    - containerd.io
+    - docker-buildx-plugin
+    - docker-compose-plugin
     - ffmpeg
     - git-lfs
     - gnuplot
@@ -537,7 +557,49 @@
     - intel-media-va-driver-non-free
     - spice-client-gtk
     - libdrm-tests
-
+    - cloud-init
+    - chrony
+    - sg3-utils
+    - util-linux-extra
+    # Runtime deps for the Intel mainline-tracking 6.18 kernel tooling .debs
+    # installed under configurations: (linux-perf, linux-kbuild, linux-misc-tools, rtla)
+    #- pahole
+    #- libconfig9
+    #- libbabeltrace1
+    #- libdebuginfod1t64
+    #- libopencsd1
+    #- libtracefs1
+    # -- Performance & Profiling Tools ------------------------------------------
+    # Intel Performance Counter Monitor - CPU/uncore/PCIe counters
+    - pcm
+    # GPU top - Intel GPU utilisation monitor (intel_gpu_top is part of intel-gpu-tools, already included above)
+    # Linux perf tool - perf top, perf stat, perf record / report
+    # linux-tools-common provides the perf wrapper script;
+    # linux-tools-generic provides the actual perf binary for the running kernel
+    - linux-tools-common
+    - linux-tools-generic
+    # Multi-purpose CPU / memory / I/O benchmark
+    - sysbench
+    # CPU, memory, I/O and device stress & benchmark suite
+    - stress-ng
+    # Flexible I/O tester for storage benchmarking
+    - fio
+    # OpenGL / GPU benchmark (glmark2)
+    - glmark2
+    # -- Intel AI / Media Stack -------------------------------------------------
+    # Intel OpenVINO Runtime & Toolkit (latest 2025.x via apt)
+    - openvino
+    # oneVPL runtime - H.264 / HEVC / AV1 HW encode & decode via VA-API
+    - libvpl2
+    # oneAPI oneDNN - runtime library + development headers for custom kernels
+    - intel-oneapi-dnnl
+    - intel-oneapi-dnnl-devel
+    # Intel RealSense SDK
+    - librealsense2-dkms
+    - librealsense2
+    - librealsense2-utils
+    - librealsense2-dev
+    - librealsense2-gl
 
   additionalFiles:
     - local: ../additionalfiles/ptl/01-network-manager-all.yaml
@@ -553,7 +615,29 @@
         - linux-image-6.18-intel_260427t075939z-r2
         - linux-headers-6.18-intel_260427t075939z-r2
 
+  users:
+    - name: user
+      password: $6$xXo2NxaLDQE6xrCa$lwcawer1BMsC63xx5jHHrVSI4625We6CMv0nzmflYiXVldJC/9gXFqYoe5KTu8lBnuc6W8MxE0eGLUUt1RiJw.
+      groups: ["sudo"]
+      hash_algo: sha512
+
   configurations:
+    - cmd: "sed -e 's@^GRUB_TIMEOUT_STYLE=hidden@# GRUB_TIMEOUT_STYLE=hidden@' -e 's@^GRUB_TIMEOUT=0@GRUB_TIMEOUT=5@g' -i /etc/default/grub"
+    - cmd: "update-grub"
+    - cmd: "update-initramfs -u"
+    # Set up APT proxies
+    - cmd: "echo 'Acquire::ftp::Proxy \"http://proxy-dmz.intel.com:911\";' > /etc/apt/apt.conf.d/99proxy.conf"
+    - cmd: "echo 'Acquire::http::Proxy \"http://proxy-dmz.intel.com:911\";' >> /etc/apt/apt.conf.d/99proxy.conf"
+    - cmd: "echo 'Acquire::https::Proxy \"http://proxy-dmz.intel.com:911\";' >> /etc/apt/apt.conf.d/99proxy.conf"
+    - cmd: "echo 'Acquire::https::proxy::*.intel.com \"DIRECT\";' >> /etc/apt/apt.conf.d/99proxy.conf"
+    # Set environment proxies
+    - cmd: "echo 'http_proxy=http://proxy-dmz.intel.com:911' >> /etc/environment"
+    - cmd: "echo 'https_proxy=http://proxy-dmz.intel.com:912' >> /etc/environment"
+    - cmd: "echo 'ftp_proxy=http://proxy-dmz.intel.com:911' >> /etc/environment"
+    - cmd: "echo 'socks_server=http://proxy-dmz.intel.com:1080' >> /etc/environment"
+    - cmd: "echo 'no_proxy=localhost,127.0.0.1,127.0.1.1,127.0.0.0/8,172.16.0.0/20,192.168.0.0/16,10.0.0.0/8,10.1.0.0/16,10.152.183.0/24,jf.intel.com,teamcity-or.intel.com,caas.intel.com,inn.intel.com,isscorp.intel.com,gfx-assets.fm.intel.com' >> /etc/environment"
+    # Enable SSH service to start automatically on boot
+    - cmd: "ln -sf /lib/systemd/system/ssh.service /etc/systemd/system/multi-user.target.wants/ssh.service"
     # Install Intel Graphics Compiler and compute runtime packages for PTL
     - cmd: "mkdir -p /tmp/gpu-packages"
     - cmd: "wget -q https://github.com/intel/intel-graphics-compiler/releases/download/v2.28.4/intel-igc-core-2_2.28.4+20760_amd64.deb -O /tmp/gpu-packages/intel-igc-core-2_2.28.4+20760_amd64.deb"
@@ -586,4 +670,51 @@
     - cmd: "wget -q https://download.01.org/intel-linux-overlay/ubuntu/linux-tools/linux-perf_6.18.23-intel+260427T075939Z_amd64.deb -O /tmp/linux-tools/linux-perf.deb"
     - cmd: "wget -q https://download.01.org/intel-linux-overlay/ubuntu/linux-tools/rtla_6.18.23-intel+260427T075939Z_amd64.deb -O /tmp/linux-tools/rtla.deb"
     - cmd: "wget -q https://download.01.org/intel-linux-overlay/ubuntu/linux-tools/usbip_2.0+6.18.23-intel+260427T075939Z_amd64.deb -O /tmp/linux-tools/usbip.deb"
-    - cmd: "find /tmp/linux-tools -maxdepth 1 -type f -name '*.deb' | xargs -r dpkg -i; apt-get install -y --fix-broken || true"
\ No newline at end of file
+    - cmd: "find /tmp/linux-tools -maxdepth 1 -type f -name '*.deb' | xargs -r dpkg -i; apt-get install -y --fix-broken || true"
+    # Configure Wayland
+    - cmd: "sed -i 's/#WaylandEnable=/WaylandEnable=/g' /etc/gdm3/custom.conf"
+    # Disable auto-updates
+    # - cmd: "sed -i 's/\"1\"/\"0\"/g' /etc/apt/apt.conf.d/20auto-upgrades"
+    # Configure bash environment
+    - cmd: "echo 'source /etc/profile.d/mesa_driver.sh' | tee -a /etc/bash.bashrc"
+    - cmd: "echo 'set enable-bracketed-paste off' >> /etc/inputrc"
+    # Configure automatic login
+    - cmd: "sed -i 's/.*AutomaticLoginEnable =.*/AutomaticLoginEnable = true/g' /etc/gdm3/custom.conf"
+    - cmd: "sed -i 's/.*AutomaticLogin = user1/AutomaticLogin = user/g' /etc/gdm3/custom.conf"
+    # Configure sudo permissions
+    - cmd: "echo 'sys_olvtelemetry ALL=(ALL) NOPASSWD: /usr/sbin/biosdecode, /usr/sbin/dmidecode, /usr/sbin/ownership, /usr/sbin/vpddecode' > /etc/sudoers.d/user-sudo"
+    - cmd: "echo 'user ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers.d/user-sudo"
+    - cmd: "chmod 440 /etc/sudoers.d/user-sudo"
+    # Configure kernel messages
+    - cmd: "echo 'kernel.printk = 7 4 1 7' > /etc/sysctl.d/99-kernel-printk.conf"
+    - cmd: "echo 'kernel.dmesg_restrict = 0' >> /etc/sysctl.d/99-kernel-printk.conf"
+    # Configure NTP and timezone for correct datetime
+    - cmd: "ln -sf /usr/share/zoneinfo/UTC /etc/localtime"
+    - cmd: "ln -sf /lib/systemd/system/chrony.service /etc/systemd/system/multi-user.target.wants/chrony.service"
+    # Set up snap proxy refresh
+    - cmd: "echo '#!/bin/bash' > /opt/snapd_refresh.sh"
+    - cmd: "echo 'snap set system proxy.http=http://proxy-dmz.intel.com:911' >> /opt/snapd_refresh.sh"
+    - cmd: "echo 'snap set system proxy.https=http://proxy-dmz.intel.com:912' >> /opt/snapd_refresh.sh"
+    - cmd: "echo 'sleep 60 && snap refresh snapd-desktop-integration' >> /opt/snapd_refresh.sh"
+    - cmd: "chmod +x /opt/snapd_refresh.sh"
+    - cmd: "(crontab -l 2>/dev/null; echo '@reboot sudo /opt/snapd_refresh.sh 2>&1 | tee /opt/snapd_refresh_logs.txt') | crontab -"
+    # Set build timestamp. Note: BUILD_TIME is captured at image build time (image creation), not at deployment/runtime.
+    - cmd: "echo \"BUILD_TIME=$(date +%Y%m%d-%H%M)\" > /opt/jenkins-build-timestamp"
+    - cmd: "echo 'PLATFORM=PTL' >> /opt/jenkins-build-timestamp"
+    - cmd: "echo 'PTL KERNEL=mainline-tracking-6.17' >> /opt/jenkins-build-timestamp"
+    # ----------------------------------------------------------------------------------
+    # Pre-install K3s and Docker CE (both disabled by default)
+    # On first boot, cloud-init reads host_type from /etc/cloud/config-file and
+    # enables the correct service:
+    #   host_type=kubernetes  ? systemctl enable --now k3s + copies kubeconfig
+    #   host_type=container   ? systemctl disable k3s, enable --now docker
+    # See additionalfiles/ptl/edge-provisioning.cfg for the cloud-init runcmd.
+    # ----------------------------------------------------------------------------------
+    # Pre-install K3s binary and service (network available during build)
+    - cmd: "curl -sfL https://get.k3s.io | INSTALL_K3S_SKIP_ENABLE=true INSTALL_K3S_SKIP_START=true INSTALL_K3S_EXEC='server --disable=traefik' sh - 2>&1 || echo Warning: K3s pre-install failed"
+    # Ensure both services are disabled by default; cloud-init activates the right one
+    - cmd: "systemctl disable k3s 2>/dev/null || true"
+    - cmd: "systemctl disable docker 2>/dev/null || true"
+    # Pre-create docker data-root and daemon config (avoids first-boot delay)
+    - cmd: "mkdir -p /opt/docker-data /etc/docker && echo '{\"data-root\": \"/opt/docker-data\"}' > /etc/docker/daemon.json"
+
