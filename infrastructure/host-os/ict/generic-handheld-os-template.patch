diff --git a/image-templates/ubuntu24-x86_64-minimal-ptl-mr1-raw.yml b/infrastructure/host-os/ict/generic-handheld-os-template.yml
--- a/image-templates/ubuntu24-x86_64-minimal-ptl-mr1-raw.yml
+++ b/infrastructure/host-os/ict/generic-handheld-os-template.yml
@@ -1,23 +1,8 @@
-# AI-searchable metadata for template discovery
-metadata:
-  description: "Ubuntu 24.04 minimal PTL MR1  desktop image in raw format for Intel PTL development and validation"
-  use_cases:
-    - "PTL platform validation"
-    - "Intel GPU and NPU enablement"
-    - "desktop development"
-    - "raw image deployment"
-  keywords:
-    - ptl
-    - mr1
-    - ubuntu
-    - raw
-    - desktop
-    - intel
-    - gpu
-    - npu
+# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
+# SPDX-License-Identifier: Apache-2.0
 
 image:
-  name: minimal-desktop-ubuntu-ptl-mr1
+  name: minimal-desktop-ubuntu
   version: "24.04"
 
 target:
@@ -29,7 +14,7 @@ target:
 # Disk layout and output artifact definition
 disk:
   # Human friendly name logged by builders
-  name: minimal-desktop-ubuntu-ptl-mr1
+  name: minimal-desktop-ubuntu
   artifacts:
     # Request conversion to raw
     - type: raw
@@ -71,7 +56,7 @@ disk:
       # Standard Linux root filesystem GUID for x86_64
       typeUUID: 4f68bce3-e8cd-4db1-96e7-fbcaf984b709
       fsType: ext4
-      start: 3073MiB
+      start: 5121MiB
       end: "0"
       mountPoint: /
       mountOptions: defaults
@@ -130,6 +115,23 @@ packageRepositories:
       - sg3-utils
       - linux-headers-6.18-intel
       - linux-image-6.18-intel
+      # -- Performance & Profiling Tools ------------------------------------------
+      #-linux-kbuild need to enable
+      #-linux-misc-tools need to enable
+      #-tmon need to enable
+      #-bootconfig need to enable
+      #-linux-config-6.18 need to enable
+      #-linux-bpf-dev need to enable
+      #-libcpupower-dev need to enable
+      #-libcpupower1 need to enable
+      - libllvm18
+      - libdebuginfod1t64
+      - linux-bpftool-6.18-intel
+      - linux-intel-pstate-tracer-6.18-intel
+      - linux-perf-6.18-intel
+      - linux-power-tools-6.18-intel
+      - linux-rtla-6.18-intel
+      - linux-usbip-6.18-intel
       # overlay dependencies
       - libvpl2
       - libavcodec62
@@ -193,6 +195,7 @@ packageRepositories:
 systemConfig:
   name: minimal
   description: Minimal desktop ubuntu image for PTL
+  hostname: minimal-desktop-ubuntu
 
   bootloader:
     bootType: efi
@@ -296,7 +299,7 @@ systemConfig:
     # resolves the transitive libssl3t64 (pulled in by many base packages) to the
     # -updates build 3.0.13-0ubuntu3.11, so libssl-dev must be pinned to the same
     # -updates version or the exact "=" dep conflicts. Do NOT also list libssl3t64
-    # explicitly ΓÇö that routes it through a path that stages the GA build instead.
+    # explicitly ├óΓé¼ΓÇ¥ that routes it through a path that stages the GA build instead.
     - libssl-dev_3.0.13-0ubuntu3.11
     - make
     - mosquitto
@@ -343,16 +346,22 @@ systemConfig:
     - chrony
     - sg3-utils
     - util-linux-extra
+    - linux-headers-6.18-intel
+    - linux-image-6.18-intel
     # -- Performance & Profiling Tools ------------------------------------------
-    # Runtime deps for the Intel mainline-tracking 6.18 kernel tooling .debs
-    # (linux-perf, linux-kbuild, linux-misc-tools, rtla)
+    #-linux-kbuild need to enable
+    #-linux-misc-tools need to enable
+    #-tmon need to enable
+    #-bootconfig need to enable
+    #-linux-config-6.18 need to enable
+    #-linux-bpf-dev need to enable
+    #-libcpupower-dev need to enable
+    #-libcpupower1 need to enable
     - libllvm18
     - pahole
     - libconfig9
     - libbabeltrace1
     - libdebuginfod1t64
-    - linux-headers-6.18-intel
-    - linux-image-6.18-intel
     - linux-bpftool-6.18-intel
     - linux-intel-pstate-tracer-6.18-intel
     - linux-perf-6.18-intel
@@ -360,11 +369,30 @@ systemConfig:
     - linux-rtla-6.18-intel
     - linux-usbip-6.18-intel
 
+    # Intel Performance Counter Monitor - CPU/uncore/PCIe counters
+    - pcm
+    # Multi-purpose CPU / memory / I/O benchmark
+    - sysbench
+    # CPU, memory, I/O and device stress & benchmark suite
+    - stress-ng
+    # Flexible I/O tester for storage benchmarking
+    - fio
+    # -- Intel AI / Media Stack -------------------------------------------------
+    # oneVPL runtime - H.264 / HEVC / AV1 HW encode & decode via VA-API
+    - libvpl2
+    # Intel RealSense SDK
+    - librealsense2-dkms
+    - librealsense2
+    - librealsense2-utils
+    - librealsense2-dev
+    - librealsense2-gl
+    # Power and Thermal daamons for power and perfromance management
+    - intel-lpmd
+    - thermald
+
   additionalFiles:
     - local: ../additionalfiles/ptl/01-network-manager-all.yaml
       final: /etc/netplan/01-network-manager-all.yaml
-    - local: ../additionalfiles/ptl/50-cloud-init.yaml
-      final: /etc/netplan/50-cloud-init.yaml
 
   kernel:
     version: "260713t060441z-r2"
@@ -373,10 +401,29 @@ systemConfig:
     packages:
       - linux-image-6.18-intel_260713t060441z-r2
       - linux-headers-6.18-intel_260713t060441z-r2
+  users:
+    - name: user
+      password: $6$xXo2NxaLDQE6xrCa$lwcawer1BMsC63xx5jHHrVSI4625We6CMv0nzmflYiXVldJC/9gXFqYoe5KTu8lBnuc6W8MxE0eGLUUt1RiJw.
+      groups: ["sudo", "video", "render", "audio"]
+      hash_algo: sha512
+
   configurations:
+    # ----------------------------------------------------------------------------------
+    # Cloud-init: restrict to NoCloud datasource only
+    # ----------------------------------------------------------------------------------
+    - cmd: "mkdir -p /etc/cloud/cloud.cfg.d"
+    - cmd: "printf 'datasource_list: [NoCloud, None]\\n' > /etc/cloud/cloud.cfg.d/99-datasource.cfg"
+    # Mask systemd-networkd-wait-online ├óΓé¼ΓÇ¥ NetworkManager-wait-online already provides network-online.target
+    - cmd: "systemctl mask systemd-networkd-wait-online.service"
+    # Purge systemd-boot ├óΓé¼ΓÇ¥ installed transitively. GRUB is the actual bootloader, so systemd-boot
+    # is unused ├óΓé¼ΓÇ¥ but its postinst (`bootctl install`) fails inside the chroot. Force-purge to recover, then pin to -1
+    # so later apt steps cannot re-pull it.
+    - cmd: "printf 'Package: systemd-boot systemd-boot-efi\\nPin: release *\\nPin-Priority: -1\\n' > /etc/apt/preferences.d/no-systemd-boot"
+    - cmd: "dpkg --purge --force-all systemd-boot systemd-boot-efi 2>/dev/null || true"
+    # Ensure GPU packages download dir exists (upstream MR1 wgets to it without mkdir)
+    - cmd: "mkdir -p /tmp/gpu-packages"
     # Intel-graphics-compiler Version: v2.34.4 (from GitHub releases, public)
     # GPU Version:  26.18.38308.1
-    - cmd: "mkdir -p /tmp/gpu-packages"
     - cmd: "wget -q https://github.com/intel/intel-graphics-compiler/releases/download/v2.34.4/intel-igc-core-2_2.34.4+21428_amd64.deb -O /tmp/gpu-packages/intel-igc-core-2_2.34.4+21428_amd64.deb"
     - cmd: "wget -q https://github.com/intel/intel-graphics-compiler/releases/download/v2.34.4/intel-igc-opencl-2_2.34.4+21428_amd64.deb -O /tmp/gpu-packages/intel-igc-opencl-2_2.34.4+21428_amd64.deb"
     - cmd: "wget -q https://github.com/intel/compute-runtime/releases/download/26.18.38308.1/intel-ocloc_26.18.38308.1-0_amd64.deb -O /tmp/gpu-packages/intel-ocloc_26.18.38308.1-0_amd64.deb"
@@ -432,7 +479,7 @@ systemConfig:
     # Pre-create docker data-root and daemon config (avoids first-boot delay)
     - cmd: 'mkdir -p /opt/docker-data /etc/docker && echo ''{"data-root": "/opt/docker-data"}'' > /etc/docker/daemon.json'
     # ----------------------------------------------------------------------------------
-    # Remove ICT-generated ubuntu-noble.list ΓÇö duplicates the Ubuntu 24.04 native
+    # Remove ICT-generated ubuntu-noble.list ├óΓé¼ΓÇ¥ duplicates the Ubuntu 24.04 native
     # ubuntu.sources (DEB822 format)
     # ----------------------------------------------------------------------------------
     - cmd: "rm -f /etc/apt/sources.list.d/ubuntu-noble.list"
